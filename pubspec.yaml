name: kakao_sdk_sample
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  kakao_flutter_sdk: ^0.5.2
  dio: ^3.0.10
  json_annotation: ^3.1.1
  package_info: ^0.4.3+2
  platform: ^2.2.1
  shared_preferences: ^0.5.12+4
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true